import { useState, type ReactNode } from "react";
import { Button } from "./button";
export function Carousel({ items, initial = 0 }: { items: ReactNode[]; initial?: number }) { const [index, setIndex] = useState(initial); if (!items.length) return null; const next = () => setIndex(i => (i + 1) % items.length); const prev = () => setIndex(i => (i - 1 + items.length) % items.length); return <div role="region" aria-roledescription="carousel" aria-label="Carousel" className="space-y-2"><div className="overflow-hidden rounded-md border">{items[index]}</div><div className="flex items-center justify-center gap-2"><Button size="sm" variant="outline" onClick={prev} aria-label="Previous slide">Previous</Button><span className="text-xs text-muted-foreground" aria-live="polite">{index + 1} / {items.length}</span><Button size="sm" variant="outline" onClick={next} aria-label="Next slide">Next</Button></div></div>; }
