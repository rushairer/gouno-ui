import { useState, type ReactElement, cloneElement } from "react";
import { AlertDialog, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "../components/primitives/alert-dialog";
import { Button } from "./button";
export function Popconfirm({ title, description, children, okText = "Confirm", cancelText = "Cancel", danger, onConfirm, onCancel, disabled }: { title:string; description?:string; children:ReactElement<{onClick?:()=>void}>; okText?:string; cancelText?:string; danger?:boolean; onConfirm?:()=>void|Promise<void>; onCancel?:()=>void; disabled?:boolean }) { const [open,setOpen]=useState(false); const [busy,setBusy]=useState(false); return <><AlertDialog open={open} onOpenChange={setOpen}><span>{cloneElement(children,{onClick:()=>{children.props.onClick?.();if(!disabled)setOpen(true)}})}</span><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>{title}</AlertDialogTitle>{description&&<AlertDialogDescription>{description}</AlertDialogDescription>}</AlertDialogHeader><AlertDialogFooter><Button disabled={busy} onClick={()=>{setOpen(false);onCancel?.()}}>{cancelText}</Button><Button loading={busy} variant={danger?"danger":"primary"} onClick={async()=>{setBusy(true);try{await onConfirm?.();setOpen(false)}finally{setBusy(false)}}}>{okText}</Button></AlertDialogFooter></AlertDialogContent></AlertDialog></>; }
