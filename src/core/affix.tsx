import type { CSSProperties, ReactNode } from "react";
export function Affix({ offsetTop = 0, children, className }: { offsetTop?: number; children: ReactNode; className?: string }) { const style: CSSProperties = { position: "sticky", top: offsetTop, zIndex: 20 }; return <div style={style} className={className}>{children}</div>; }
export function BackTop({ visibilityHeight = 200, children = "↑", className }: { visibilityHeight?: number; children?: ReactNode; className?: string }) { return <button type="button" aria-label="Back to top" onClick={() => window.scrollTo({ top: 0, behavior: "smooth" })} data-visibility-height={visibilityHeight} className={`fixed bottom-6 right-6 z-40 flex size-11 items-center justify-center rounded-full bg-primary text-primary-foreground shadow-overlay ${className || ""}`}>{children}</button>; }