import { Button } from "./button";
export function Pagination({ page, total, pageSize = 10, onChange, disabled }: { page: number; total: number; pageSize?: number; onChange?: (page: number) => void; disabled?: boolean }) { const pages = Math.max(1, Math.ceil(total / pageSize)); return <nav aria-label="Pagination" className="flex items-center gap-1"><Button size="sm" variant="outline" disabled={disabled || page <= 1} onClick={() => onChange?.(page - 1)}>Previous</Button><span className="px-2 text-sm" aria-live="polite">{page} / {pages}</span><Button size="sm" variant="outline" disabled={disabled || page >= pages} onClick={() => onChange?.(page + 1)}>Next</Button></nav>; }
