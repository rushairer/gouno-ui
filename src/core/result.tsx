import type { ReactNode } from "react";
export function Result({ status = "info", title, subTitle, extra, children }: { status?: "success"|"error"|"info"|"warning"; title: ReactNode; subTitle?: ReactNode; extra?: ReactNode; children?: ReactNode }) { return <section role="status" className="flex flex-col items-center gap-3 p-8 text-center"><div className={`flex size-12 items-center justify-center rounded-full text-xl ${status === "success" ? "bg-success-subtle text-success" : status === "error" ? "bg-danger-subtle text-destructive" : status === "warning" ? "bg-warning-subtle text-warning" : "bg-info-subtle text-info"}`}>{status === "success" ? "✓" : status === "error" ? "!" : status === "warning" ? "!" : "i"}</div><h2 className="text-xl font-semibold">{title}</h2>{subTitle && <p className="text-sm text-muted-foreground">{subTitle}</p>}{extra}{children}</section>; }
