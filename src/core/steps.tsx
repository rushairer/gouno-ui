import type { ReactNode } from "react";
export function Steps({ items, current = 0, orientation = "horizontal" }: { items: { title: ReactNode; description?: ReactNode }[]; current?: number; orientation?: "horizontal"|"vertical" }) { return <ol className={orientation === "vertical" ? "flex flex-col gap-4" : "flex items-start gap-4 overflow-auto"}>{items.map((item, i) => <li key={i} className="flex min-w-0 items-start gap-2" aria-current={i === current ? "step" : undefined}><span className={`flex size-6 shrink-0 items-center justify-center rounded-full text-xs ${i <= current ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground"}`}>{i + 1}</span><span className="min-w-0"><strong className="block text-sm">{item.title}</strong>{item.description && <span className="text-xs text-muted-foreground">{item.description}</span>}</span></li>)}</ol>; }
