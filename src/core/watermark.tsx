import type { CSSProperties, ReactNode } from "react";
export function Watermark({ content = "Gouno", children, rotate = -22, gap = 120, opacity = 0.12 }: { content?: string; children: ReactNode; rotate?: number; gap?: number; opacity?: number }) { const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="${gap}" height="${gap}"><text x="10" y="60" transform="rotate(${rotate} 60 60)" fill="rgba(0,0,0,${opacity})" font-size="16">${content.replace(/[<>&]/g, "")}</text></svg>`; const style: CSSProperties = { backgroundImage: `url("data:image/svg+xml,${encodeURIComponent(svg)}")` }; return <div className="relative" style={style}>{children}</div>; }
