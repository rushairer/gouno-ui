import { useState, type ReactNode } from "react";
export function Segmented<T extends string>({ options, value, defaultValue, onChange, disabled, ariaLabel = "Segmented control" }: { options: readonly ({ label: ReactNode; value: T; disabled?: boolean }|T)[]; value?: T; defaultValue?: T; onChange?: (value: T) => void; disabled?: boolean; ariaLabel?: string }) { const normalized = options.map(x => typeof x === "string" ? { label: x, value: x as T } : x); const [inner, setInner] = useState(defaultValue ?? normalized[0]?.value); const current = value ?? inner; return <div role="radiogroup" aria-label={ariaLabel} className="inline-flex rounded-md bg-muted p-1">{normalized.map(option => <button type="button" role="radio" aria-checked={current === option.value} key={option.value} disabled={disabled || option.disabled} onClick={() => { if (value === undefined) setInner(option.value); onChange?.(option.value); }} className={`rounded px-3 py-1.5 text-sm ${current === option.value ? "bg-background shadow-sm" : "text-muted-foreground"}`}>{option.label}</button>)}</div>; }
