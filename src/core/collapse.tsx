import { useState, type ReactNode } from "react";
export function Collapse({ items, defaultActiveKeys = [], accordion = false }: { items: { key: string; label: ReactNode; children: ReactNode; disabled?: boolean }[]; defaultActiveKeys?: string[]; accordion?: boolean }) { const [active, setActive] = useState(defaultActiveKeys); return <div className="divide-y rounded-md border">{items.map(item => { const open = active.includes(item.key); return <section key={item.key}><button type="button" disabled={item.disabled} aria-expanded={open} className="flex w-full items-center justify-between p-4 text-left font-medium disabled:opacity-50" onClick={() => setActive(keys => open ? keys.filter(k => k !== item.key) : accordion ? [item.key] : [...keys, item.key])}>{item.label}<span aria-hidden="true">{open ? "−" : "+"}</span></button>{open && <div className="px-4 pb-4 text-sm text-muted-foreground">{item.children}</div>}</section>; })}</div>; }
