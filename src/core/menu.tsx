import type { ReactNode } from "react";
export interface MenuItem { key: string; label: ReactNode; icon?: ReactNode; disabled?: boolean; danger?: boolean; }
export function Menu({ items, selectedKeys = [], onSelect, mode = "vertical", ariaLabel = "Menu" }: { items: MenuItem[]; selectedKeys?: string[]; onSelect?: (key: string) => void; mode?: "vertical"|"horizontal"; ariaLabel?: string }) { return <nav aria-label={ariaLabel}><ul role="menu" className={mode === "horizontal" ? "flex items-center gap-1" : "flex flex-col gap-1"}>{items.map(item => <li key={item.key} role="none"><button role="menuitem" type="button" disabled={item.disabled} onClick={() => onSelect?.(item.key)} className={`flex w-full items-center gap-2 rounded-md px-3 py-2 text-sm ${selectedKeys.includes(item.key) ? "bg-accent text-accent-foreground" : "hover:bg-accent"} ${item.danger ? "text-destructive" : ""}`}>{item.icon}{item.label}</button></li>)}</ul></nav>; }
