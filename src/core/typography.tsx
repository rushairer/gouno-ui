import type { HTMLAttributes, ElementType, ReactNode } from "react";
import { cn } from "../lib/utils";
export function Heading({ level = 2, children, className, ...props }: HTMLAttributes<HTMLHeadingElement> & { level?: 1|2|3|4|5|6; children?: ReactNode }) { const Tag = `h${level}` as ElementType; return <Tag {...props} className={cn(level === 1 ? "text-3xl font-semibold tracking-tight" : level === 2 ? "text-2xl font-semibold tracking-tight" : level === 3 ? "text-xl font-semibold" : "text-lg font-semibold", className)}>{children}</Tag>; }
export function Text({ as: Component = "p", size = "md", tone = "default", children, className, ...props }: HTMLAttributes<HTMLElement> & { as?: ElementType; size?: "xs"|"sm"|"md"|"lg"; tone?: "default"|"muted"|"danger"|"success"; children?: ReactNode }) { return <Component {...props} className={cn(size === "xs" ? "text-xs" : size === "sm" ? "text-sm" : size === "lg" ? "text-lg" : "text-base", tone === "muted" && "text-muted-foreground", tone === "danger" && "text-destructive", tone === "success" && "text-success", className)}>{children}</Component>; }
